// server.js - CosmoDance Chat Bot v3.0 (Полная база знаний)
import express from "express";
import cors from "cors";
import dotenv from "dotenv";
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";

dotenv.config();

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

// ============ ИНИЦИАЛИЗАЦИЯ ============
const app = express();
app.use(cors());
app.use(express.json({ limit: "100kb" }));
app.use(express.static(__dirname));

// ============ ЗАГРУЗКА БАЗЫ ЗНАНИЙ ============
function loadKnowledge() {
  try {
    const data = fs.readFileSync("knowledge.json", "utf8");
    return JSON.parse(data);
  } catch (error) {
    console.error("❌ Ошибка загрузки knowledge.json:", error.message);
    return { 
      docs: [],
      info: {
        website: "https://cosmo.su/",
        schedule_link: "https://cosmo.su/raspisanie/",
        prices_link: "https://cosmo.su/prices/",
        contacts_link: "https://cosmo.su/contacts/"
      }
    };
  }
}

const KNOWLEDGE = loadKnowledge();
console.log(`📚 Загружено ${KNOWLEDGE.docs?.length || 0} документов из базы знаний`);

// ============ ПОИСК В БАЗЕ ЗНАНИЙ ============
function searchInKnowledge(query) {
  if (!KNOWLEDGE.docs || !Array.isArray(KNOWLEDGE.docs)) {
    return null;
  }
  
  const lowerQuery = query.toLowerCase();
  
  // Сначала ищем точные совпадения в заголовках
  const exactTitleMatch = KNOWLEDGE.docs.find(doc => 
    doc.title && doc.title.toLowerCase().includes(lowerQuery)
  );
  
  if (exactTitleMatch) {
    return exactTitleMatch;
  }
  
  // Ищем в тексте
  const textMatch = KNOWLEDGE.docs.find(doc => 
    doc.text && doc.text.toLowerCase().includes(lowerQuery)
  );
  
  if (textMatch) {
    return textMatch;
  }
  
  // Ищем по тегам
  const tagMatch = KNOWLEDGE.docs.find(doc => 
    doc.tags && doc.tags.some(tag => 
      tag && tag.toLowerCase().includes(lowerQuery)
    )
  );
  
  if (tagMatch) {
    return tagMatch;
  }
  
  // Разбиваем запрос на слова
  const queryWords = lowerQuery.split(' ').filter(word => word.length > 3);
  
  for (const doc of KNOWLEDGE.docs) {
    const docText = (doc.title + ' ' + doc.text + ' ' + (doc.tags?.join(' ') || '')).toLowerCase();
    const matchCount = queryWords.filter(word => docText.includes(word)).length;
    
    if (matchCount >= Math.max(1, queryWords.length / 2)) {
      return doc;
    }
  }
  
  return null;
}

// ============ ЛОКАЛЬНЫЕ ОТВЕТЫ ============
const LOCAL_RESPONSES = {
  // Приветствия
  'привет': '👋 Привет! Я чат-бот студии танцев CosmoDance. Чем могу помочь?',
  'здравствуйте': '👋 Здравствуйте! Я ассистент студии CosmoDance.',
  'добрый день': '👋 Добрый день! Рад вас видеть в студии CosmoDance!',
  'доброе утро': '👋 Доброе утро! Готов ответить на ваши вопросы.',
  'добрый вечер': '👋 Добрый вечер! Чем могу помочь?',
  'start': '🎯 **Добро пожаловать в CosmoDance!**\n\nЯ помогу вам:\n• Узнать цены и абонементы\n• Найти ближайший филиал\n• Узнать о направлениях\n• Записаться на занятие\n\nЧто вас интересует?',
  
  // Быстрые ссылки
  'сайт': `🌐 **Наш сайт:** ${KNOWLEDGE.info?.website || 'https://cosmo.su/'}\nЗдесь вся актуальная информация!`,
  'расписание': `📅 **Расписание:** ${KNOWLEDGE.info?.schedule_link || 'https://cosmo.su/raspisanie/'}\n\n🎯 **Всегда проверяйте актуальное расписание на сайте!**`,
  'цены': `💰 **Цены и абонементы:**\n${KNOWLEDGE.info?.prices_link || 'https://cosmo.su/prices/'}\n\n💎 **Самые свежие цены и акции только на сайте!**`,
  'контакты': `📞 **Контакты всех филиалов:**\n${KNOWLEDGE.info?.contacts_link || 'https://cosmo.su/contacts/'}`,
  
  // Филиалы
  'филиалы': '📍 **Наши филиалы в Санкт-Петербурге:**\n\n1. **Звёздная** (м. Звёздная)\n2. **Дыбенко** (м. Пр. Большевиков)\n3. **Купчино** (м. Купчино)\n4. **Озерки** (м. Озерки)\n\n🔗 **Все адреса и контакты:** https://cosmo.su/contacts/',
  
  // ЦЕНЫ (точные ответы из базы знаний)
  'сколько стоит пробное': '💎 **Пробное занятие:**\n\n• Стоимость: 400 ₽\n• Это первое посещение направления\n• При покупке абонемента становится БЕСПЛАТНЫМ\n\n🎯 **Идеально, чтобы попробовать перед покупкой абонемента!**',
  
  'пробное занятие': '💎 **Пробное занятие:**\n\n• Стоимость: 400 ₽\n• Первое посещение любого направления\n• При покупке абонемента — в подарок (бесплатно)\n\n🎯 **Рекомендуем сначала посетить пробное!**',
  
  'разовое занятие': '🎫 **Разовое занятие:**\n\n• Стоимость: 900 ₽\n• Разовое посещение без абонемента\n• Идеально, если хотите просто попробовать\n\n💡 **Выгоднее купить абонемент!**',
  
  'абонемент на месяц': '📅 **Абонементы на 1 месяц:**\n\n**На 60 минут:**\n• 4 занятия — 3 290 ₽\n• 8 занятий — 4 990 ₽\n• Безлимит — 11 500 ₽\n\n**На 85 минут:**\n• 4 занятия — 3 950 ₽\n• 8 занятий — 6 500 ₽\n\n💎 **При покупке пробное занятие бесплатно!**',
  
  'абонемент на 8 занятий': '📊 **Абонемент на 8 занятий:**\n\n**На 60 минут:**\n• 8 занятий — 4 990 ₽\n• Срок: 1 месяц\n\n**На 85 минут:**\n• 8 занятий — 6 500 ₽\n• Срок: 1 месяц\n\n🎯 **Самый популярный вариант!**',
  
  'сколько стоит абонемент': '💰 **Абонементы CosmoDance:**\n\n**На 60 минут (1 час):**\n• 4 занятия — 3 290 ₽ (1 месяц)\n• 8 занятий — 4 990 ₽ (1 месяц)\n• 12 занятий — 6 500 ₽ (1 месяц)\n• 12 занятий — 7 200 ₽ (2 месяца)\n• 24 занятия — 12 590 ₽ (3,5 месяца)\n• 48 занятий — 22 900 ₽ (6,5 месяца)\n• Безлимит — 11 500 ₽ (1 месяц)\n\n**На 85 минут (1,5 часа):**\n• 4 занятия — 3 950 ₽ (1 месяц)\n• 8 занятий — 6 500 ₽ (1 месяц)\n• 12 занятий — 9 200 ₽ (2 месяца)\n• 24 занятия — 17 200 ₽ (3,5 месяца)\n• 48 занятий — 31 650 ₽ (6,5 месяца)\n\n💎 **Пробное занятие: 400 ₽ (бесплатно при покупке абонемента)**',
  
  'безлимит': '∞ **Безлимитный абонемент:**\n\n• Стоимость: 11 500 ₽\n• Срок: 1 месяц\n• Неограниченное количество посещений\n\n🎯 **Идеально, если планируете ходить чаще 3 раз в неделю!**',
  
  'индивидуальное занятие': '👤 **Индивидуальные занятия:**\n\n• Стоимость: от 2 200 ₽\n• Цена зависит от направления и хореографа\n• Точную стоимость сообщает администратор\n\n💎 **Персональный подход и максимальный результат!**',
  
  'абонемент для ребенка': '👧 **Абонементы для детей:**\n\n• Цены такие же, как для взрослых\n• При покупке абонемента ребёнку дарится дневник танцора\n\n**На 60 минут:**\n• 4 занятия — 3 290 ₽\n• 8 занятий — 4 990 ₽\n• 12 занятий — 6 500 ₽\n\n🎁 **Специальный подарок для детей!**',
  
  'продлить абонемент': '⏰ **Продление абонемента:**\n\nДа, абонемент можно продлить при предоставлении:\n• Медицинской справки\n• Больничного листа\n\n📞 **Обратитесь к администратору для продления.**',
  
  'сколько длится занятие': '⏱️ **Длительность занятий:**\n\n• Часовое занятие = 60 минут\n• Полуторачасовое занятие = 85 минут\n\n🎯 **Уточняйте длительность у конкретного направления!**',
  
  'все цены одинаковые': '✅ **Единые цены:**\n\nДа, все цены одинаковы для всех направлений студии CosmoDance.\nНет разделения по стилям танца.\n\n💰 **Одна цена для всех направлений!**',
  
  // Филиалы с конкретной информацией
  'звездн': '📍 **Филиал на Звёздной:**\n\n🚇 **Метро:** Звёздная\n• 5-7 минут пешком от метро\n• Современные залы\n• Раздевалки и душевые\n• Все группы для начинающих\n\n🔗 **Точный адрес:** https://cosmo.su/contacts/#zvezdnaya',
  
  'дыбенк': '📍 **Филиал на Дыбенко:**\n\n🚇 **Метро:** Проспект Большевиков\n• Удобное расположение\n• Большие залы\n• Парковка рядом\n\n🔗 **Точный адрес:** https://cosmo.su/contacts/#dybenko',
  
  'купчин': '📍 **Филиал в Купчино:**\n\n🚇 **Метро:** Купчино\n• Современное оборудование\n• Комфортные раздевалки\n• Зона отдыха\n\n🔗 **Точный адрес:** https://cosmo.su/contacts/#kupchino',
  
  'озерк': '📍 **Филиал в Озерках:**\n\n🚇 **Метро:** Озерки\n• Светлые залы\n• Новые зеркала\n• Современная вентиляция\n\n🔗 **Точный адрес:** https://cosmo.su/contacts/#ozerki',
  
  // Направления и общая информация
  'начать': '🎯 **Как начать танцевать в CosmoDance:**\n\n1. **Выберите направление** которое вам нравится\n2. **Найдите ближайший филиал**\n3. **Посмотрите расписание** для начинающих\n4. **Запишитесь на пробное занятие** (400 ₽)\n5. **Приходите в удобной одежде**\n\n📅 **Расписание:** https://cosmo.su/raspisanie/\n💰 **Цены:** https://cosmo.su/prices/',
  
  'новичок': '🎯 **Для новичков:**\n\n✅ **Не нужно подготовки** — все начинают с нуля!\n✅ **Индивидуальный подход** к каждому\n✅ **Дружелюбная атмосфера**\n✅ **Опытные тренеры**\n\n🔥 **Популярные направления для начинающих:**\n• Hip-Hop\n• Jazz Funk\n• Contemporary\n• High Heels\n• Latina\n\n📅 **Выберите и приходите!**',
  
  'записаться': '📝 **Как записаться:**\n\n**Способы записи:**\n1. **На сайте** через форму записи\n2. **По телефону** филиала\n3. **В соцсетях** студии\n4. **Лично** в студии\n\n🎯 **Перед записью:**\n• Посмотрите расписание\n• Выберите удобный филиал\n• Определитесь с направлением\n\n🔗 **Форма записи на сайте:** https://cosmo.su/',
  
  'направлен': '💃 **Направления танцев:**\n\n🎯 **Для начинающих доступны:**\n• Hip-Hop (хип-хоп)\n• Jazz Funk (джаз-фанк)\n• Contemporary (контемпорари)\n• High Heels (высокие каблуки)\n• Latina (латина)\n• Twerk (тверк)\n• Strip Dance (стрип-пластика)\n• Break Dance (брейк-данс)\n\n🔗 **Все направления:** https://cosmo.su/directions/',
  
  'что нужно': '🎒 **Для первого занятия:**\n\n1. **Одежда:** удобная, не сковывающая\n2. **Обувь:** чешки, кроссовки или носки\n3. **Вода:** бутылка с водой\n4. **Полотенце:** по желанию\n5. **Хорошее настроение!**\n\n💎 **Все остальное предоставляет студия.**',
  
  'тренер': '👨‍🏫 **Наши тренеры:**\n\n• Профессиональные хореографы\n• Опыт работы от 5 лет\n• Участие в чемпионатах\n• Индивидуальный подход к новичкам\n\n🔥 **Все тренеры специализируются на работе с начинающими!**\n🔗 **Подробнее:** https://cosmo.su/trainers/'
};

// ============ МАРШРУТЫ API ============

// Главная страница
app.get("/", (req, res) => {
  res.sendFile(path.join(__dirname, "index.html"));
});

// Чат с ботом
app.post("/chat", async (req, res) => {
  try {
    const { message } = req.body;
    const userMessage = (message || "").trim();
    
    if (!userMessage) {
      return res.json({
        reply: "👋 Пожалуйста, напишите ваш вопрос о студии CosmoDance."
      });
    }

    console.log(`📨 Запрос: "${userMessage}"`);
    
    const lowerMessage = userMessage.toLowerCase();
    
    // 1. Проверяем локальные ответы
    for (const [key, response] of Object.entries(LOCAL_RESPONSES)) {
      if (lowerMessage.includes(key) && key.length > 3) {
        console.log(`✅ Используем локальный ответ для: ${key}`);
        return res.json({ 
          reply: response,
          source: "local_response"
        });
      }
    }
    
    // 2. Ищем в базе знаний
    const knowledgeMatch = searchInKnowledge(userMessage);
    if (knowledgeMatch) {
      console.log(`✅ Найдено в базе знаний: ${knowledgeMatch.title}`);
      
      let response = '';
      
      // Форматируем ответ в зависимости от категории
      switch (knowledgeMatch.category) {
        case 'prices':
          response = `💰 **${knowledgeMatch.title}**\n\n${knowledgeMatch.text}\n\n`;
          response += `💎 **Актуальные цены всегда на сайте:** ${KNOWLEDGE.info?.prices_link || 'https://cosmo.su/prices/'}`;
          break;
          
        case 'branches':
          response = `📍 **${knowledgeMatch.title}**\n\n${knowledgeMatch.text}\n\n`;
          response += `📞 **Контакты и точный адрес:** ${KNOWLEDGE.info?.contacts_link || 'https://cosmo.su/contacts/'}`;
          break;
          
        case 'schedule':
          response = `📅 **${knowledgeMatch.title}**\n\n${knowledgeMatch.text}\n\n`;
          response += `🎯 **Актуальное расписание:** ${KNOWLEDGE.info?.schedule_link || 'https://cosmo.su/raspisanie/'}`;
          break;
          
        default:
          response = `🎯 **${knowledgeMatch.title}**\n\n${knowledgeMatch.text}`;
          if (knowledgeMatch.category === 'directions') {
            response += `\n\n🔗 **Все направления:** ${KNOWLEDGE.info?.directions_link || 'https://cosmo.su/directions/'}`;
          }
      }
      
      return res.json({
        reply: response,
        source: "knowledge_base"
      });
    }
    
    // 3. Общий ответ с ссылками
    const generalResponse = `🎯 **Студия танцев CosmoDance**\n\n` +
      `К сожалению, я не нашел точного ответа в базе знаний.\n\n` +
      `**Попробуйте задать вопрос по-другому:**\n` +
      `• "Сколько стоит пробное занятие?"\n` +
      `• "Какие абонементы есть на 8 занятий?"\n` +
      `• "Как добраться до филиала на Звёздной?"\n` +
      `• "Какие направления есть для начинающих?"\n\n` +
      `📅 **Расписание:** ${KNOWLEDGE.info?.schedule_link || 'https://cosmo.su/raspisanie/'}\n` +
      `💰 **Цены:** ${KNOWLEDGE.info?.prices_link || 'https://cosmo.su/prices/'}\n` +
      `📍 **Контакты:** ${KNOWLEDGE.info?.contacts_link || 'https://cosmo.su/contacts/'}`;
    
    return res.json({
      reply: generalResponse,
      source: "general_info"
    });

  } catch (error) {
    console.error("❌ Ошибка обработки запроса:", error.message);
    
    res.json({ 
      reply: `🎯 **Студия танцев CosmoDance**\n\n` +
             `📍 **Филиалы:** Звёздная, Дыбенко, Купчино, Озерки\n` +
             `📅 **Расписание:** ${KNOWLEDGE.info?.schedule_link || 'https://cosmo.su/raspisanie/'}\n` +
             `💰 **Цены:** ${KNOWLEDGE.info?.prices_link || 'https://cosmo.su/prices/'}\n` +
             `🌐 **Сайт:** ${KNOWLEDGE.info?.website || 'https://cosmo.su/'}\n\n` +
             `📞 **Свяжитесь с нами для записи и консультации.**`,
      error: true,
      source: "error_fallback"
    });
  }
});

// Просмотр базы знаний (для отладки)
app.get("/api/knowledge", (req, res) => {
  const { search, category } = req.query;
  
  let results = KNOWLEDGE.docs || [];
  
  if (search) {
    results = results.filter(doc => 
      doc.title.toLowerCase().includes(search.toLowerCase()) ||
      doc.text.toLowerCase().includes(search.toLowerCase()) ||
      (doc.tags && doc.tags.some(tag => tag.toLowerCase().includes(search.toLowerCase())))
    );
  }
  
  if (category) {
    results = results.filter(doc => doc.category === category);
  }
  
  res.json({
    search,
    category,
    total_docs: KNOWLEDGE.docs?.length || 0,
    count: results.length,
    results: results.map(doc => ({
      id: doc.id,
      title: doc.title,
      category: doc.category,
      tags: doc.tags,
      excerpt: doc.text.substring(0, 100) + (doc.text.length > 100 ? '...' : '')
    }))
  });
});

// Список всех категорий
app.get("/api/categories", (req, res) => {
  const categories = [...new Set(KNOWLEDGE.docs?.map(d => d.category).filter(Boolean))];
  res.json({
    categories,
    count: categories.length
  });
});

// Статистика
app.get("/health", (req, res) => {
  const categories = [...new Set(KNOWLEDGE.docs?.map(d => d.category).filter(Boolean))];
  
  res.json({
    status: "healthy",
    service: "CosmoDance Chat Bot",
    version: "3.0",
    timestamp: new Date().toISOString(),
    last_updated: KNOWLEDGE.last_updated,
    knowledge_base: {
      total_docs: KNOWLEDGE.docs?.length || 0,
      categories: categories,
      categories_count: categories.length
    },
    features: {
      local_responses: Object.keys(LOCAL_RESPONSES).length,
      knowledge_search: true,
      no_external_dependencies: true
    },
    links: KNOWLEDGE.info
  });
});

// Админ: добавить документ в базу знаний
app.post("/api/knowledge/add", (req, res) => {
  try {
    const { title, text, category, tags } = req.body;
    
    if (!title || !text) {
      return res.status(400).json({ error: "Требуются title и text" });
    }
    
    const newDoc = {
      id: `doc_${Date.now()}`,
      title,
      text,
      category: category || "general",
      tags: tags || [],
      created_at: new Date().toISOString()
    };
    
    KNOWLEDGE.docs.push(newDoc);
    KNOWLEDGE.last_updated = new Date().toISOString();
    
    // Сохраняем в файл
    fs.writeFileSync(
      path.join(__dirname, "knowledge.json"),
      JSON.stringify(KNOWLEDGE, null, 2)
    );
    
    res.json({
      success: true,
      message: "Документ добавлен",
      doc: newDoc
    });
    
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

// ============ ЗАПУСК СЕРВЕРА ============
const port = process.env.PORT || 10000;
const host = process.env.HOST || '0.0.0.0';

app.listen(port, host, () => {
  console.log("=".repeat(60));
  console.log("🚀 CosmoDance Chat Bot v3.0 ЗАПУЩЕН!");
  console.log(`📍 Порт: ${port}`);
  console.log(`🌐 Хост: ${host}`);
  console.log(`🔗 URL: http://${host}:${port}`);
  console.log(`📚 База знаний: ${KNOWLEDGE.docs?.length || 0} документов`);
  console.log(`💰 Категории: ${[...new Set(KNOWLEDGE.docs?.map(d => d.category).filter(Boolean))].join(', ')}`);
  console.log("=".repeat(60));
  console.log("✅ Бот готов отвечать на вопросы о ценах, филиалах и занятиях!");
});

// Обработка завершения
process.on('SIGTERM', () => {
  console.log('🔄 Получен SIGTERM, завершаем работу...');
  process.exit(0);
});

process.on('SIGINT', () => {
  console.log('🔄 Получен SIGINT, завершаем работу...');
  process.exit(0);
});
